// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("cld", "for cld support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("cld", "enable cld support", "no");

if (PHP_CLD != "no") {
	EXTENSION("cld", "cld.c", PHP_EXTNAME_SHARED, "/DZEND_ENABLE_STATIC_TSRMLS_CACHE=1");
}

